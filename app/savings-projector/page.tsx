import { SavingsProjector } from "../components/savings-projector";

export const metadata = { title: "Tính Tiết Kiệm — EB3VIET" };

export default function SavingsProjectorPage() {
  return <SavingsProjector />;
}
