---
name: playwright-test-cases-promote
description: Promote approved draft test cases from test-tasks workspace to tests/playwright/test-cases/ in the repo. Use after reviewing draft test cases generated by test-cases command.
user-invocable: true
when_to_use: "Invoke to promote approved draft test cases to the tests/playwright/test-cases/ directory."
category: test
keywords: [playwright, test-cases, promote, qa, workspace]
---

# Playwright Test Cases Promote

## Arguments

- `--task=<task-id>` — Task ID (REQUIRED)
- `--page=<page-name>` — Promote specific page only (optional, default: all pages)

---

## ⛔ STOP — Do This First

Use the `Read` tool now before anything else:

`Read(".claude/skills/playwright-test-cases-promote/references/promote.md")`

This file is the ONLY source of truth. Do not execute anything before reading it.

After reading, execute all steps in that file. The arguments the user passed (--task, --page) are already in your context — use them in STEP 1.

## Execution

Follow the step-by-step process in → See Read() call in STOP section above.

## Operating Rules

- Always read draft files before comparing with repo files
- Use hash comparison to detect changes — never blindly overwrite
- Ask user for each changed file — never auto-overwrite without confirmation
- New files (not in repo) are copied directly without asking
- Unchanged files are silently skipped
- After promotion, update task README to mark "Test Cases" as promoted
