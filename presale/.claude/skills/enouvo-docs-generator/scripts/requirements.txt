# Enouvo Document Generator Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
markdown>=3.4.0
weasyprint>=60.0
pypdf>=3.0.0
Pillow>=9.0.0
reportlab>=4.0.0
Pygments>=2.15.0  # Syntax highlighting for code blocks
pymdown-extensions>=10.0  # Better fenced code blocks (handles indented blocks in lists)
python-docx>=1.0.0  # Word document generation for DOCX converter

# Note: WeasyPrint requires system dependencies:
# - macOS: brew install cairo pango gdk-pixbuf libffi
# - Ubuntu: apt-get install libcairo2 libpango-1.0-0 libgdk-pixbuf2.0-0

# Optional: For mermaid diagram rendering
# Install separately: npm install -g @mermaid-js/mermaid-cli
